import React from 'react';

function RatingsBar() {
  return (
    <div id="ratingsBar">
      <span id="positiveProgress" />
      <span id="negativeProgress" />
    </div>
  );
}

export default RatingsBar;
